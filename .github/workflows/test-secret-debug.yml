name: Test MYTOKEN secret
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print secret length
        run: |
          echo "Token length: ${#MYTOKEN}"
          echo "Token first 4: ${MYTOKEN:0:4}"
        env:
          MYTOKEN: ${{ secrets.MYTOKEN }}
