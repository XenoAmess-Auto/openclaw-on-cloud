# 2026-03-06

## OOC 平台聊天串房间问题修复（增强版）

**问题**: 不同群的聊天消息串了（一个房间的消息显示到另一个房间）

**根本原因**: 
当用户切换房间时，WebSocket session 可能由于竞态条件或多次 join 请求，没有正确从旧房间的 session 集合中移除，导致同一个 session 同时存在于多个房间，从而收到多个房间的消息。

**修复方案（第一轮）**:
1. 在 `WebSocketBroadcastService.registerRoomSession()` 中，先调用 `removeSessionFromAllRooms()` 确保 session 从所有其他房间移除，再注册到新房间
2. 添加 `checkForCrossRoomSessions()` 调试日志，用于检测和诊断串房间问题

**修复方案（第二轮 - 增强）**:
1. **添加全局同步锁 (`sessionLock`)**: 保护所有 session 注册/移除操作，确保线程安全
2. **运行时串房间检测与自动修复**: `broadcastToRoom()` 现在会检测并修复同时存在于多个房间的 session
3. **防御性拷贝**: 广播前创建 session 集合的副本，避免并发修改异常
4. **增强日志**: 使用 `[SessionRegistry]`、`[CROSS-ROOM]`、`[Broadcast]` 前缀便于追踪

**修改文件**: 
- `backend/src/main/java/com/ooc/websocket/WebSocketBroadcastService.java`
- `backend/src/main/java/com/ooc/websocket/ChatWebSocketHandler.java`

**提交**: 
- `7c021a8` - 第一轮修复
- `5ee027d` - 第二轮增强修复（auto-deploy）

**部署状态**: ✅ 后端已重启，服务正常运行

**验证**: 代码已编译部署，等待用户验证是否还有串房间问题
